// Injected by Vite define — stamped at build time in vite.config.ts.
// Declared as string | undefined so that typeof guards in source files
// type-check correctly; Vite always provides the value in real builds.
declare const __BUILD_DATE__: string | undefined;
